1.  What is Hoisting? Uses.
A.  Hoisting is a mechanism by that if we want to access any variable or function before its declaration so we can.
    But only with var & normal function not with let, const, arrow Function becz 



2.  Is javascript a dynamically typed language or a statically typed language ?
A.  Correct flow:
    JavaScript
     JS Code
       ↓
    Parsed + JIT compiled/interpreted by engine (like V8)
       ↓
    Runs in browser/node
       ↓
    Type checking happens while running (Runtime)
    Example:
    let a = 10;
    a = "Hello"; // allowed at runtime
    JavaScript engine checks values during execution.

    TypeScript
    TS Code
      ↓
    Compile Time Checking
      ↓
    Converted (transpiled) into JavaScript
      ↓
    JavaScript runs
    Example:
    let a: number = 10;

    Important thing:
    TypeScript does not convert directly into binary.
    It first converts into JavaScript:
    TypeScript → JavaScript → Machine-level execution



3.  What is Spread & Rest Operator in JavaScript?
A.  The Spread Operator (...) is used to expand/spread elements of an array, object, or string into individual values.
    Syntax-
    ...data
    Uses
    1. Merge Arrays
    let a = [1,2];
    let b = [3,4];
    let c = [...a, ...b];
    console.log(c); // [1,2,3,4]

    2. Copy Array
    let arr = [1,2,3];
    let copy = [...arr];
    console.log(copy); // [1,2,3]

    3. Pass Array as Function Arguments
    function demo(x,y,z){
      console.log(x,y,z);
    }
    let nums = [10,20,30];
    demo(...nums);

    Rest Operator -
    The Rest Operator (...) is used to collect multiple values into a single array.
    Syntax-
    ...data
    Uses
    1. Function Parameters
    function demo(...args){
      console.log(args);
    }
    demo(1,2,3,4);
    // [1,2,3,4]
    2. Remaining Elements
    let [a, ...rest] = [1,2,3,4];
    console.log(rest); // [2,3,4]



4.  What is Destructuring? Uses.
A.  Destructuring is an ES6 feature that allows us to extract (unpack) values from objects or arrays and assign them to variables in a single step.
   🔹Ex:
    const user = { name: "Aman", age: 23 };
    const { name, age } = user;
    console.log(name, age);

    const colors = ['red', 'green', 'blue'];
    const [first, second] = colors; // first = 'red', second = 'green'

    💡One-line interview version:
    👉“Destructuring is used to unpack values from arrays or objects into variables in a concise way.”
